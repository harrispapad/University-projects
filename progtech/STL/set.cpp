/*
#include <iostream>
#include <set>

using namespace std;

int main(){
    set<int> s;

    while (true){
        int x;
        cin >> x;

        if (s.find(x) == s.end())
            s.insert(x);
        else {
            cout << "You lose, I've seen " << x << " before!" << endl;
            exit(1);
        }
    }
}

 */
